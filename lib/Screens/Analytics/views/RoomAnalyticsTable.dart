import 'package:flutter/material.dart';

class Roomanalyticstable extends StatelessWidget {
  const Roomanalyticstable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}